# Home.py
import streamlit as st
from pathlib import Path

st.set_page_config(
    layout="wide",
    page_title="Trade Dashboard",
    page_icon="📊"
)

st.title("📊 Trade Dashboard")
st.markdown("""
Добро пожаловать в панель трейдера! Выбери раздел в меню слева.

### 📈 Что доступно:
- **Анализ доходности** — сравнение инструментов, факторный анализ
- **Статистический анализ** — отклонения от средней, Z-оценки, процентили

### ⚙️ Настройки:
- Все настройки сохраняются автоматически
- Подключение к БД через SSH туннель
""")

# Проверка конфига
config_file = Path("last_run_config.json")
if config_file.exists():
    st.success("✅ Конфигурация найдена")
else:
    st.info("ℹ️ Конфигурация будет создана при первом запуске")

st.divider()
st.markdown("**Совет:** Начни с *Анализа доходности*, чтобы выбрать интересные тикеры, затем иди в *Статистический анализ* для поиска точек входа.")